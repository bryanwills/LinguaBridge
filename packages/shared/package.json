{
  "name": "@linguabridge/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, Zod schemas, and language utilities.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo \"lint handled at app level\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
