# Copy to infra/docker/.env.web on the VPS and fill in real values.
# Used both as build args (NEXT_PUBLIC_*) and runtime env for the container.

# App
NEXT_PUBLIC_APP_URL=https://talk.bryanwills.dev

# Supabase (browser-safe)
NEXT_PUBLIC_SUPABASE_URL=https://alwmofeyqtloizzdgpck.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_9qiNUmTeV2zlCwUL-ABzdg_MHLCD7PV

# Supabase (server-only; from dashboard -> Settings -> API. Not needed for
# auth/dashboard, required by later phases.)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# LiveKit (Phase 3 — leave empty for now)
NEXT_PUBLIC_LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_HTTP_URL=

# Retention
DEFAULT_RETENTION_DAYS=30
MAX_RETENTION_DAYS=30
RETENTION_CRON_SECRET=
